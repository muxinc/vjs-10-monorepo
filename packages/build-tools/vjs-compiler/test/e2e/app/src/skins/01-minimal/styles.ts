/**
 * Level 1: Minimal Styles
 *
 * Only basic utilities:
 * - Padding (p-4, p-2)
 * - Border radius (rounded-full)
 * - Background colors (bg-white, bg-blue-500)
 * - Layout (absolute, inset-0, flex, items-center, justify-center)
 */

export const styles = {
  Container: 'relative',
  Controls: 'absolute inset-0 flex items-center justify-center',
  Button: 'p-4 rounded-full bg-white/90 hover:bg-white',
};
