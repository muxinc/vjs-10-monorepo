---
import type { HTMLTag, Polymorphic } from 'astro/types';
import { clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

type Props<Tag extends HTMLTag = 'ul'> = Polymorphic<{ as: Tag }> & {
  maxWidth?: boolean;
  class?: string;
};

const { as: Tag = 'ul', maxWidth = true, class: className, ...props } = Astro.props;
---

<Tag
  class={twMerge(clsx('list-disc list-outside pl-5 mb-5 space-y-1', maxWidth && 'max-w-3xl mx-auto'), className)}
  {...props}
>
  <slot />
</Tag>
